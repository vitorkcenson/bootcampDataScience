# Get the user's name as input
name = input("Please, enter your name: ") 
# Prompts the user to enter their name.
# Stores the entered name in the variable name.

# Print the user's name using string formatting
print("Your name is: {}.".format(name)) 
# Uses string formatting (.format()) to insert the value of name into the string.
# Prints the formatted string, displaying the user's name.

# Get the user's age as input and convert it to an integer
age = int(input("Now enter your age please: ")) 
# Prompts the user to enter their age.
# Stores the entered age as a string.

# Print the user's age using string formatting
print("Your age is: {}.".format(age)) 
# Uses string formatting to insert the value of age into the string.
# Prints the formatted string, displaying the user's age.

# Print a general greeting message
print("Hello, world!") 